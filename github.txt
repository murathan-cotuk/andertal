git clone https://github.com/murathan-cotuk/banzano

cmd: git checkout dev && git add . && git commit -m "update" && git push origin dev
ps: git checkout dev ; git add . ; git commit -m "update" ; git push origin dev

    git checkout dev
    git add .
    git commit -m "Yeni özellik eklendi"
    git push origin dev


cmd: git checkout main && git merge dev && git push origin main
ps: git checkout main ; git merge dev ; git push origin main

    git checkout main
    git merge dev
    git push origin main
    
.\git-workflow.ps1

Hotinger eski degerler:
A	@	0   2.57.91.91  50	
CNAME	www	0	banzano.com	300
